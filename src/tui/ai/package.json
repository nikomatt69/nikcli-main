{
  "name": "tui-ai",
  "version": "0.1.7",
  "description": "AI-specific components and streaming for TUI applications",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo 'Tests not implemented yet'",
    "validate:tsc": "tsc --noEmit -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "ai": "^3.4.33",
    "blessed": "^0.1.81",
    "tui-core": "./core",
    "tui-providers": "./providers"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "tui",
    "terminal",
    "ai",
    "chat",
    "streaming",
    "blessed",
    "typescript"
  ]
}

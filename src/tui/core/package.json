{
  "$schema": "https://json.schemastore.org/package",
  "name": "nikcli-tui",
  "version": "0.2.0",
  "description": "Core TUI components and primitives",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo 'Tests not implemented yet'",
    "validate:tsc": "tsc --noEmit -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "blessed": "^0.1.81",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "tui",
    "terminal",
    "ui",
    "components",
    "blessed",
    "typescript",
    "shadcn"
  ]
}
{
  "name": "tui-agents",
  "version": "0.1.7",
  "description": "Agent system and specialized agents",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "validate:tsc": "tsc --noEmit -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "rimraf": "^6.0.1"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "tui-ai": "./ai"
  }
}